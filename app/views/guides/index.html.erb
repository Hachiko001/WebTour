<p id="notice"><%= notice %></p>

<h1>Guides</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Password</th>
      <th>Date</th>
      <th>Language</th>
      <th>Address</th>
      <th>Email</th>
      <th>Image</th>
      <th>Rating</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guides.each do |guide| %>
      <tr>
        <td><%= guide.name %></td>
        <td><%= guide.password %></td>
        <td><%= guide.date %></td>
        <td><%= guide.language %></td>
        <td><%= guide.address %></td>
        <td><%= guide.email %></td>
        <td><%= image_tag guide.image_url %></td>
        <td><%= guide.rating %></td>
        <td><%= link_to 'Show', guide %></td>
        <td><%= link_to 'Edit', edit_guide_path(guide) %></td>
        <td><%= link_to 'Destroy', guide, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guide', new_guide_path %>
